Screenshots for the main README. Generate them by running:

1. Start the app: `python app.py`
2. In another terminal: `pip install playwright && playwright install chromium && python scripts/capture_screenshots.py`

This creates `main.png`, `search.png`, and `rating-modal.png`.
